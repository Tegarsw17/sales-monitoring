const userRouter = require('./users-route')
const itemRouter = require('./items-route')
const imageItemRouter = require('./image_items-route')


module.exports = {
    userRouter,
    itemRouter,
    imageItemRouter,
}